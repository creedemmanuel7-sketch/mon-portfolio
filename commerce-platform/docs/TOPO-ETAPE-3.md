# TOPO — Étape 3 : Développement

## Objectif
Transformer le prototype UI en app **Vite + React + TypeScript + Tailwind**, catalogue **40 produits**, panier réel (state), prêt pour Supabase + Stripe.

## Livré dans cette itération
1. +30 produits (images + data) → **40 au total**
2. Data partagée `data/products.json`
3. Scaffold app `app/` (Vite/React/TS/Tailwind)
4. Pages boutique : home, catalogue, produit, panier, checkout (simulation)
5. `.env.example` pour Supabase + Stripe (clés à brancher)

## Ensuite (itération suivante Étape 3b)
- Auth Supabase + table `orders`
- Stripe Checkout Session + webhook
- Staging deploy Netlify/Vercel
